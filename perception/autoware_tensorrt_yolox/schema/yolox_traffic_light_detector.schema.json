{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "tensorrt_traffic_light_detector parameter",
  "type": "object",
  "definitions": {
    "yolox_traffic_light_detector": {
      "type": "object",
      "properties": {
        "dla_core_id": {
          "type": "number",
          "default": -1,
          "description": "If positive ID value is specified, the node assign inference task to the DLA core."
        },
        "gpu_id": {
          "type": "integer",
          "default": 0,
          "description": "GPU ID for selecting CUDA device"
        },
        "calibration_image_list_path": {
          "type": "string",
          "default": "",
          "description": "Path to a file which contains path to images. Those images will be used for int8 quantization."
        },
        "is_roi_overlap_segmentation": {
          "type": "boolean",
          "description": "Refine segmentation by overlaying ROI class."
        },
        "overlap_roi_score_threshold": {
          "type": "number",
          "description": "Minimum probability of detected ROI to consider for replacing segmentation."
        },
        "is_publish_color_mask": {
          "type": "boolean",
          "description": "Publish color mask for result visualization."
        }
      },
      "required": [
        "dla_core_id",
        "gpu_id",
        "calibration_image_list_path",
        "is_roi_overlap_segmentation",
        "overlap_roi_score_threshold",
        "is_publish_color_mask"
      ],
      "additionalProperties": false
    }
  },
  "properties": {
    "/**": {
      "type": "object",
      "properties": {
        "ros__parameters": {
          "$ref": "#/definitions/yolox_traffic_light_detector"
        }
      },
      "required": ["ros__parameters"],
      "additionalProperties": false
    }
  },
  "required": ["/**"],
  "additionalProperties": false
}
