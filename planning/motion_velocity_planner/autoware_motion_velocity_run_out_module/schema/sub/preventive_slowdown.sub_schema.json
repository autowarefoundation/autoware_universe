{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Run Out module configuration for the preventive slowdown feature",
  "type": "object",
  "definitions": {
    "preventive_slowdown": {
      "type": "object",
      "description": "Parameters for comfortable slowdown behavior.",
      "properties": {
        "on_time_buffer": {
          "type": "number",
          "description": "[s] Successive collision detection time required to start the slowdown decision.",
          "default": 0.1
        },
        "off_time_buffer": {
          "type": "number",
          "description": "[s] Successive non-collision detection time required to remove the slowdown decision.",
          "default": 0.1
        },
        "distance_buffer": {
          "type": "number",
          "description": "[m] Longitudinal distance between the collision and the slowdown positions.",
          "default": 2.0
        }
      },
      "required": ["on_time_buffer", "off_time_buffer", "distance_buffer"],
      "additionalProperties": false
    }
  }
}
