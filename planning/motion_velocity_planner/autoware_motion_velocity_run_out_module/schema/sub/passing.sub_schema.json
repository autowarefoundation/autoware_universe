{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Run Out module configuration for the passing feature",
  "type": "object",
  "definitions": {
    "passing": {
      "type": "object",
      "description": "Parameters to ignore collisions under certain conditions.",
      "properties": {
        "enable_passing_margin": {
          "type": "boolean",
          "description": "If true, a collision where ego arrives first is ignored based on the time_margin.",
          "default": true
        },
        "time_margin": {
          "type": "number",
          "description": "[s] Margin to decide if ego passes first (higher is more conservative). Only used if enable_passing_margin is true.",
          "default": 1.5
        },
        "max_overlap_duration": {
          "type": "number",
          "description": "[s] The collision is not ignored if ego is predicted to stay on the object's path for longer than this duration.",
          "default": 2.0
        }
      },
      "required": ["enable_passing_margin", "time_margin", "max_overlap_duration"],
      "additionalProperties": false
    }
  }
}
