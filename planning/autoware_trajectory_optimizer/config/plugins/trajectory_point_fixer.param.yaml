/**:
  ros__parameters:
    trajectory_point_fixer:
      # Orientation threshold for removing wrongly oriented points
      orientation_threshold_deg: 5.0  # [deg] Yaw threshold for removing wrongly oriented points

      # Distance thresholds for point processing
      min_dist_to_remove_m: 0.01  # [m] Minimum distance to remove close proximity points
      min_dist_to_merge_m: 0.025   # [m] Minimum distance to merge close proximity points
